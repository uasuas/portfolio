<div class="container">
  <div class="row">
    <div class="col">
      <h2>企業情報ページ</h2>
      <div class="row">
        <div class="side-content col-12 col-lg-4">
          <div class="side-content-sticky row">
            <div class="image_tag col-12 col-lg-12 col-sm-6">
              <%= image_tag @company.get_company_image(200,200) %>
            </div>
            <%= render "layouts/company-table", company: @company %>
          </div>
        </div>
        <div class="col-12 col-lg-8">
          <div class="row">
            <div class="col-12">
              <h2>プラン一覧</h2>
            </div>
          </div>
          <% @company.line_plans.each do |plan| %>
              <div class="card new-badge" onclick="window.location='<%= line_plan_path(plan) %>'" role="link" style="cursor: pointer;" onmouseover="this.style.backgroundColor='#f0f0f0';" onmouseout="this.style.backgroundColor='';">
                <div class="card-body">
                  <h5 class="card-title"><%= plan.name %></h5>
                  <p class="card-text">月額料金：<%= format_monthly_fee(plan.monthly_fee) %>円</p>
                  <p class="card-text">
                    プラン内容：
                    <!--mapでプランに紐ずく回線種類情報を取得し配列を作成、その後「,」で区切りを付けたつの文字列に連結して表示する。-->
                    <% contents = plan.contents.map(&:content).join(', ') %>
                    <%= contents %>
                  </p>
                  <p class="card-text">
                    対応地域：
                    <!--mapでプランに紐ずくエリア情報を取得し配列を作成、その後「,」で区切りを付けたつの文字列に連結して表示する。-->
                    <% areas = plan.areas.map(&:area).join(', ') %>
                    <%= areas %>
                  </p>
                  <h5 class="card-title">プラン説明</h5>
                  <p class="card-text"><%= plan.introduction %></p>
                </div>
              </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>